# Memória - 2026-04-22 (Quarta-feira)

## 🛠️ Refatoração do Sistema
- **Nova Estrutura:** Lógica em `~/TermAI` | Dados em `~/.TermAI`.
- **Caminhos:** Padronizados para usar a home do usuário via `os.getenv("HOME")`.
- **Status:** Operacional para leitura e escrita de arquivos.

## 🔍 Diagnóstico Técnico (Bug do io.popen)
- **Problema:** O `io.popen` do Lua no Termux falha silenciosamente ao tentar listar (`ls`, `find`) conteúdos de pastas que começam com ponto (`.`).
- **Evidência:** `executar_bash` funciona, mas a ferramenta `listar_workspace` (que usa a mesma base) retorna vazio.
- **Conclusão:** Bloqueio de stream do SO para processos Lua em diretórios ocultos.

## ⚙️ Configurações
- **Modelo:** google/gemma-4-31b-it:free
- **Contexto:** 262.144 tokens.
- **Flush:** Configurado para cada 40k tokens.
]]

**Passo 6: Finalização.**

[FLUSH_DONE
